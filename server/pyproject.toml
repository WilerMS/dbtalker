[project]
name = "server"
version = "0.1.0"
description = "DBTalker FastAPI backend"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.135.1",
    "jsonwebtoken>=1.0.0",
    "sse-starlette>=3.3.3",
    "uvicorn[standard]>=0.42.0",
]

[tool.uv]
package = false
